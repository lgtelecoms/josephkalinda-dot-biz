/**
 * Public copy and UI strings are sourced from `src/messages/*`.
 * Use `getMessages(locale)` from `@/lib/i18n` in server components and pages.
 */
export { getMessages, isLocale } from "./i18n";
export type { Locale, Messages } from "./i18n";
